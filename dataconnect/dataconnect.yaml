specVersion: "v1beta"
serviceId: "idc-app"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "idc-app-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
